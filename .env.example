# Local dev: leave VITE_API_BASE unset so requests use `/admin` and the Vite proxy (vite.config.ts).
# Do NOT set http://localhost:8080/admin unless you also fix CORS on the Spring app.
#
# VITE_API_BASE=/admin
# VITE_API_TIMEOUT=30000
